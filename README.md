# 🧠 Автоматизация работы с интерактивной отчётностью Газпромбанка

## 📄 Описание  
Скрипт автоматизирует ввод **ИСД** на сайте интерактивной отчётности Газпромбанка с помощью Selenium WebDriver.  
Программа:
1. Загружает список ИСД из Excel-файла.  
2. Авторизуется на сайте, используя логин и пароль из `.env`.  
3. Последовательно вводит ИСД в поисковое поле и проверяет, найден ли элемент.  
4. Формирует Excel-отчёт с результатами проверки.  
5. Показывает уведомление Windows о завершении работы.

---

## ⚙️ Настройка окружения

### 1. Установка зависимостей
```bash
pip install selenium pandas python-dotenv winotify openpyxl
```

### 2. Переменные окружения
Создайте в папке со скриптом файл **.env** со следующим содержимым:
```
LOGIN=ваш_логин
PASSWORD=ваш_пароль
```
Пароль и логин хранятся в `.env`, чтобы не сохранять их в коде напрямую.  
Файл `.env` не рекомендуется коммитить в репозиторий.

---

## 📁 Параметры в коде

| Переменная | Описание |
|-------------|-----------|
| `path_excel` | Путь к Excel-файлу, где в столбце **ИСД** содержатся идентификаторы. |
| `interval_input_ISD` | Интервал (в секундах) между вводом ИСД и проверкой результата. По умолчанию: `6`. |
| `links` | Словарь с доступными ссылками. Можно выбрать одну из них. |
| `link = links.get(1)` | Определяет, какая ссылка используется: `0` — первая, `1` — вторая. |
| `path_to_save` | Путь, по которому сохраняется итоговый файл `output.xlsx` с результатами. |

---

## 🧾 Формат входного файла

**Excel-файл** должен содержать столбец:
```
ИСД
1234567
7654321
...
```
Скрипт автоматически добавляет ведущие нули до 7 знаков.

---

## 📊 Выходные данные

После выполнения создаётся файл **output.xlsx**, содержащий:

| № | ИСД | Результат |
|---|-----|-----------|
| 1 | 1234567 | True |
| 2 | 7654321 | False |

---

## 🔐 Логин и пароль
Для безопасности данные авторизации берутся из файла `.env`:
```python
login = os.getenv("LOGIN")
password = os.getenv("PASSWORD")
```
Это защищает учётные данные от случайной публикации.

---

## ⏱️ Интервал
Параметр:
```python
interval_input_ISD = 6
```
определяет паузу между вводом каждого ИСД.  
Если сайт отвечает медленно, увеличьте значение, например до `10`.

---

## 🌐 Выбор ссылки
Для работы предусмотрено две ссылки:
```python
links = {
    0: 'https://bs-interactive.gazprombank.ru/ir2spa/ir/1948/price-dynamic-pao',
    1: 'https://bs-interactive.gazprombank.ru/ir2spa/ir/233/price-dynamic-pao/'
}
```
Выбор осуществляется строкой:
```python
link = links.get(1)
```
Где:
- `0` — первая ссылка,  
- `1` — вторая.

---

## 📋 Логи
Во время выполнения скрипт выводит подробные логи в консоль:
- время запуска и завершения,
- статус авторизации,
- процесс заполнения ИСД,
- найден ли каждый элемент.

Пример вывода:
```
12:45:02 Программа запущена
12:45:05 Открываем сайт
12:45:16 Переходим на страницу "Интерактивная отчетность"
***
12:45:26 Процесс заполнения ИСД
***
1. ИСД № 1234567 найден в списке.
2. ИСД № 2345678 **не найден** в списке!
13:01:03 Программа завершена
Время выполнения 15.4
```

---

## 🔔 Уведомления
После завершения работы появляется уведомление Windows:
> 💬 *"Ваша программа завершена."*

---

## 🧱 Требования
- Python 3.10+
- Microsoft Edge установлен в системе  
- Драйвер Edge совместимой версии (Selenium автоматически его запускает, если в PATH)
